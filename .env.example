# Optional. Enables Gemini powered natural language search.
# Get a key from https://aistudio.google.com/apikey and copy this file to .env.local
# Without a key the app falls back to the on-device query parser.
VITE_GEMINI_API_KEY=
